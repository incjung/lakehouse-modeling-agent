# =============================================
# Lakehouse Modeling Agent - 의존성 목록
# Python >= 3.10 권장
# =============================================

# ── Phase 2: 소스 데이터 프로파일링 ──────────────────
pandas>=1.5.0          # CSV/Parquet/JSON 로드, 데이터 분석
pyarrow>=10.0.0        # .parquet 파일 읽기/쓰기 (pandas 백엔드)

# ── Phase 4: Mock 데이터 생성 ─────────────────────────
# pandas (위에서 포함)

# ── Phase 5: 샌드박스 검증 (DuckDB) ──────────────────
duckdb>=0.9.0          # 로컬 SQL 샌드박스 검증 엔진

# ── Phase 6: ETL 코드 생성 (운영 환경) ───────────────
# pyspark는 로컬 실행이 아닌 운영 환경에서 사용합니다.
# 로컬 개발/테스트가 필요한 경우 아래 주석을 해제하세요:
# pyspark>=3.3.0
